<div class="user-name">
  <p><%= link_to gravatar_for(current_user, size: 50), current_user %></p>
  <h1 class="title is-4"><%= current_user.name %></h1>
</div>
<p>
  <%= link_to "view my profile", current_user %>, 
  <%= pluralize(current_user.microposts.count, "micropost") %>
</p>
